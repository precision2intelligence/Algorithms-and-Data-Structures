# -*- coding:utf-8 -*-
class Solution:
    # array 二维列表
    def Find(self, target, array):
        # write code here
        flag = 'false'
        row=0
        col=len(array[0])-1
        while (row<=len(array)-1) and (col>=0):#不加while的括号可以提高运行速度
             if array[row][col]==target:
                flag = 'true'
                return flag
             elif array[row][col]>target:
                col=col-1
             else :
                row=row+1
        return flag

while True:
    try:
        S=Solution()
        # 字符串转为list
        L=list(eval(raw_input()))
        array=L[1]
        target=L[0]
        print(S.Find(target, array))
    except:
        break



#我写的另一个程序，没有调通
# -*- coding:utf-8 -*-
class Solution:
    # array 二维列表
    def Find(self, target, array):
        # write code here
        flag='false'
        row=len(array)-1
        col=0
        if (row>=0) and (col>=0):
            for x in array[row][col::]:
                if x==target:
                    flag='true'
                    return flag
                elif x>target:
                     row=row-1
                else: 
                     col=col-1
        return flag

while True:
    try:
        number=input()
        a=number[0]
        b=number[1]
        f=Solution()
        print f.Find(a,b)
    except:
        break

